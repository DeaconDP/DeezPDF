# Copy to .env.local for local dev (never commit real values).
# Web search proxy (dev/preview only — see README)
BRAVE_SEARCH_API_KEY=

# Production build: remote PDF search endpoint (optional)
# VITE_PDF_SEARCH_URL=https://example.com/api/pdf-search
